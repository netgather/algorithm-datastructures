散列表(哈希表)
  散列表是一种动态集合结构，支持insert、search、delete字典操作，是实现字典操作的一种有效数据结构。
  尽管在最坏的情况下，散列表中查找一个元素的时间与链表中查找的时间相同，达到了O(n)。然而，在实际应用
  中，散列查找的性能是极好的。在一些合理的假设下，在一个散列表中查找一个元素的平均时间是O(1)。
  在散列表中，不是直接把关键字作为数组的下标，而是根据关键字计算出相应的下标
  冲突：多个关键字映射到数组的同一个下标


